## New things leaned:

    - event.currentTarget. -> return the element that the event was called on (form, button ..etc)
    - Conditional rendering.
    - Short-circuit rendering (bool && <Component />).
    - Spread syntax (...array).
    - Passing state as a parameter to a component.